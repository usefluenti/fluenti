---
title: Code Splitting
description: Choose a build-time locale chunk strategy and wire runtime lazy locale loading
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components'

`fluenti compile` always generates tree-shakeable locale modules — every message is a `/* @__PURE__ */` annotated export so bundlers can eliminate unused translations. Fluenti splits this into two separate concerns:

- **Build-time strategy** via `splitting` in `fluenti.config.ts`
- **Runtime locale loading** via `loadMessages()` or `chunkLoader`

## How It Works

1. Run `fluenti compile` to generate per-locale ES modules with named exports and `@__PURE__` annotations
2. Each message becomes a tree-shakeable named export — unused messages are removed from the production bundle
3. Pick a build-time strategy with `splitting: 'dynamic' | 'static' | false`
4. If you want async locale switches, configure `loadMessages()` or `chunkLoader`
5. When `setLocale()` is called, the locale chunk is loaded before switching

## 1. Pick a build-time strategy

Configure the Vite build strategy in `fluenti.config.ts`:

```typescript
import { defineConfig } from '@fluenti/cli'

export default defineConfig({
  splitting: 'dynamic',
  defaultBuildLocale: 'en',
})
```

| Strategy | What ships | Best for |
|----------|------------|----------|
| `false` | All locales bundled together | Small apps or very small catalogs |
| `'static'` | One build locale inlined directly into the bundle | SSR / SSG when each build only needs one locale |
| `'dynamic'` | Default build locale in the initial bundle, other locales as separate chunks | SPAs or multi-locale apps |

## 2. Compile catalogs

Compile your catalogs:

```bash
npx fluenti compile
```

This generates:
- `locales/compiled/en.js` — named exports per message with `export default` re-export
- `locales/compiled/zh-CN.js` — same exports, translated
- `locales/compiled/index.js` — locale list and lazy loaders

The Vite plugin reads the compiled locale modules automatically; you do not need to import internal virtual modules yourself.

## 3. Enable runtime lazy locale loading

Configure your framework runtime if you want `setLocale()` to fetch locale chunks on demand:

<Tabs>
<TabItem label="Vue">
```typescript
const i18n = createFluentVue({
  locale: 'en',
  messages: { en },
  lazyLocaleLoading: true,
  chunkLoader: (locale) => import(`./locales/compiled/${locale}.js`),
})
```
</TabItem>

<TabItem label="React">
```tsx
<I18nProvider
  locale="en"
  messages={{ en }}
  loadMessages={(locale) => import(`./locales/compiled/${locale}.js`)}
>
  <App />
</I18nProvider>
```
</TabItem>

<TabItem label="Solid">
```tsx
<I18nProvider
  locale="en"
  messages={{ en }}
  lazyLocaleLoading={true}
  chunkLoader={(locale) => import(`./locales/compiled/${locale}.js`)}
>
  <App />
</I18nProvider>
```
</TabItem>
</Tabs>

If you do not configure `loadMessages()` / `chunkLoader`, `setLocale()` only switches between already-loaded catalogs. That is still valid with any build-time `splitting` strategy.

## Build vs Dev Mode

| Mode | Behavior |
|------|----------|
| **Dev** | All locales loaded eagerly for instant HMR |
| **Build** | Only `defaultBuildLocale` is bundled; others are lazy chunks |

## Preload on Hover

<Aside type="tip">
Call `preloadLocale()` on hover so the catalog is already cached when the user clicks. The switch feels instant.
</Aside>

<Tabs>
<TabItem label="Vue">
```vue
<template>
  <button
    v-for="loc in locales"
    :key="loc"
    @mouseenter="preloadLocale(loc)"
    @click="setLocale(loc)"
  >
    {{ loc }}
  </button>
</template>

<script setup>
import { useI18n } from '@fluenti/vue'
const { setLocale, preloadLocale } = useI18n()
const locales = ['en', 'ja', 'zh-CN']
</script>
```
</TabItem>

<TabItem label="React">
```tsx
import { useI18n } from '@fluenti/react'

function LocaleSwitcher() {
  const { setLocale, preloadLocale } = useI18n()
  const locales = ['en', 'ja', 'zh-CN']

  return locales.map((loc) => (
    <button
      key={loc}
      onMouseEnter={() => preloadLocale(loc)}
      onClick={() => setLocale(loc)}
    >
      {loc}
    </button>
  ))
}
```
</TabItem>

<TabItem label="Solid">
```tsx
import { useI18n } from '@fluenti/solid'

function LocaleSwitcher() {
  const { setLocale, preloadLocale } = useI18n()
  const locales = ['en', 'ja', 'zh-CN']

  return locales.map((loc) => (
    <button
      onMouseEnter={() => preloadLocale(loc)}
      onClick={() => setLocale(loc)}
    >
      {loc}
    </button>
  ))
}
```
</TabItem>
</Tabs>
