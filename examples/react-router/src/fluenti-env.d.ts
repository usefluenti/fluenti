/**
 * Global tagged template function transformed by @fluenti/vite-plugin.
 * Transforms `t`message`` into `i18n.t('message')` at build time.
 */
declare function t(strings: TemplateStringsArray, ...values: unknown[]): string
