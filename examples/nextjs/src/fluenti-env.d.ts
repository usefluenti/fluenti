/**
 * Global tagged template function injected by @fluenti/next webpack loader.
 * Transforms `t`message`` into `__i18n.t('message')` at build time.
 */
declare function t(strings: TemplateStringsArray, ...values: unknown[]): string
